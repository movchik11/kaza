import 'package:flutter/material.dart';

class LockWrapper extends StatelessWidget {
  final Widget child;
  const LockWrapper({super.key, required this.child});

  @override
  Widget build(BuildContext context) => child;
}
